# 《人格牌》AI 人格生成系统策划决策记录

## 1. 文档定位

- 本文记录第 3、6、9 关结束后生成 AI 人格牌的最新策划确认。
- 本文只冻结已经确认的产品规则，并列出仍待数值锚定的相似人格参数；当前阶段不代表已经完成代码接入。
- 本文涉及的 AI 权限高于《AI 接入与玩法兼容规则规格》中“AI 只负责表现文案”的旧边界，但仍必须遵守本地白名单、本地数值和本地校验规则。
- 世界观、正式命名、风味描述和人格台词继续保持 `UNDECIDED`，不阻塞规则容器开发。

## 2. 首版生成位置与产出

1. 第 3、6、9 关结束后各触发一次 AI 人格生成，一局共触发 3 次。
2. 每个节点只生成并展示 1 张新人格牌，不改成三选一。
3. 新人格显示在当前“获得新的人格牌”界面的“本次获得”区域。
4. 玩家可以用新人格替换当前装备，也可以不替换；不提供重新生成。
5. 节点结果首次生成后立即锁定并写入活动局状态，读档不得重抽。
6. AI 或网络失败时，从本地合法备用人格池抽取 1 张并继续流程，不取消本次奖励。

## 3. 三种内部生成方向

AI 人格生成使用以下三种内部方向：

- **顺势**：强化玩家已经形成的核心打法。
- **桥接**：保留一部分现有打法条件，同时连接另一种牌型、花色、行动或得分来源，重点形成 Combo。
- **破局**：不依赖玩家当前最主要的打法，为手牌不顺或构筑单一路线提供另一种可用解法。

以上名称只用于内部生成和调试：

- 正式界面不得显示“顺势”“桥接”“破局”等分类标签。
- 不得把行为标签、算法分类、匹配分、生成理由或开发参数一股脑展示给玩家。
- 正式界面只显示玩家需要理解和操作的信息：人格图片、人格名称或临时编号、触发条件、生效效果、成长信息和装备选择。

## 4. 三个生成节点的方向安排

1. 第 3 关与第 6 关不生成顺势人格。
2. 第 3 关与第 6 关分别覆盖桥接和破局，两类在一局内各出现一次；每局开始时随机交换两类的先后顺序。
3. 本局的桥接／破局顺序由本地随机结果确定并随活动局存档锁定，读档不得重新交换。
4. 正式界面不得告诉玩家本次或下次人格属于桥接、破局或顺势，也不得显示三类的排列顺序。
5. 第 9 关生成顺势人格。此时使用前九关累计行为、近期行为和主动构筑选择判断玩家已经形成的主要打法。
6. 破局人格不得使用玩家当前牌库和资源几乎无法触发的条件；破局代表提供替代路线，不代表惩罚或废掉现有构筑。

## 5. AI 规则权限：方案 B 正式运行，方案 A 负责扩库

### 5.1 正式运行权限

1. 正式游戏采用方案 B：AI 只能从本地批准的合法零件中组合人格。
2. 合法零件包括触发条件、附加条件、生效效果、作用目标、成长规则、强度档位和兼容标签。
3. AI 不得直接输出自由战斗数值、代码、未知条件、未知效果或本地运行时无法识别的规则。
4. 具体数值由本地价值锚定表确定；AI 只能选择合法强度档位，最终结果必须通过本地校验。
5. 来自 AI 的组合没有通过查重、冲突、可触发性、数值预算或结构校验时，整张候选失效并使用本地合法结果。

### 5.2 后续规则扩库

1. 方案 A 保留为游戏外的规则研发工具，允许 AI 提出当前零件库尚不能表达的新规则。
2. 自由提案不得直接进入玩家存档或正式战斗。
3. 新提案必须依次经过策划审核、可执行性拆解、数值锚定、本地实现和自动测试。
4. 审核通过的新机制转成新的合法零件后，才能由方案 B 在正式游戏中组合使用。
5. 后续外部规则数据库只作为研究和扩库输入，不直接作为正式可执行配置。

## 6. 人格成长与商店人格锻造

1. 每张 AI 生成人格都必须具备成长能力，不生成完全没有成长路径的普通效果牌。
2. 成长方式可以不同，包括但不限于累计触发成长；其他成长方式必须进入合法成长规则池并通过本地校验。
3. AI 生成人格必须接入现有商店“人格锻造”流程，不能只让现有 8 张基础人格使用该功能。
4. 每张 AI 生成人格需要具备与基础人格一致的词条结构：
   - 核心触发条件；
   - 主属性；
   - 第二词条槽；
   - 第三词条槽。
5. 第二、第三词条的候选池、解锁价格、解锁次数、互斥和强度仍由本地配置决定，AI 不得自由填写。
6. 商店展示、购买、解锁、存档和战斗结算都必须识别动态 AI 人格实例，不得继续只按 8 张固定人格 ID 处理。

## 7. 跨局保留与局内成长

1. AI 生成人格可以进入永久收藏，不是只在当前活动局临时生效。
2. 人格生成时确定的核心触发条件和初始主属性跨局保留，这是该人格的固定身份和固定初始强度。
3. 玩家以后需要在人格收藏／装备界面手动装备 AI 人格，不自动覆盖现有装备。
4. AI 人格和现有系统人格遵守同一跨局规则，不为两类人格维护不同的成长存档逻辑。
5. 商店人格锻造解锁的第二、第三词条只在当前活动局生效；活动局结束后不写入永久收藏，新一局重新进入未解锁状态。
6. 累计触发成长及其产生的属性增量全部属于当前活动局养成；新一局开始时全部重置，不存在永久触发成长。
7. 局内临时状态全部重置，包括触发计数、成长计数、成长属性增量、连续计数、蓄力和临时加成。
8. 永久人格带入新一局时，直接恢复该人格生成或获得时确定的核心触发条件和初始主属性，不额外计算“开局强度”。
9. 局内养成继续覆盖牌型、单卡以及人格第二、第三词条三个方向。
10. 永久收藏保存的是人格本体，不保存上一局的培养结果。

## 8. 重复检查

### 8.1 检查范围

每次生成时必须检查：

- 当前装备的人格；
- 本局已经获得的人格；
- 本局生成后被放弃的人格；
- 永久收藏中的人格。

### 8.2 权重原则

1. 当前装备和本局已经获得的人格具有最高查重权重，新人格必须具有明显的触发、效果或作用方式差异。
2. 本局被放弃的人格参与查重，但影响低于当前装备和本局已获得人格。
3. 永久收藏人格参与查重；完全相同的规则不得再次生成。
4. 永久人格连续 3 个已结算活动局未装备后，可以降低相似度惩罚，允许生成触发相近但效果更优或结构不同的高品质人格；通关和失败的活动局都计入局数。
5. 每个活动局结算时检查该人格是否曾进入任意装备槽：本局曾经装备则视为已使用，并把连续未装备局数重置为 0；整局从未装备则连续未装备局数 +1。已经装备但没有成功触发，仍然视为已使用。
6. 高品质相似人格暂时允许作为独立人格共存，不显示“同源”“进阶”等额外标记。
7. 后续融合系统上线后，再处理高相似人格的合并、继承或消耗；当前阶段不提前实现。

### 8.3 机制指纹

程序需要为人格建立机制指纹，至少包含：

- 核心触发类型及参数；
- 附加条件；
- 效果类型及作用目标；
- 成长方式；
- 牌型、花色、行动、资源、人格联动等玩法标签；
- 品质和强度档位。

只改变名称、图片或少量数值，但机制指纹基本一致的候选，仍按高度重复处理。

## 9. 行为依据

本地行为系统先把玩家操作整理成聚合快照，再交给 AI 选择合法组合。首版至少覆盖：

- 牌型使用次数、贡献得分和连续使用情况；
- 花色使用比例、得分贡献和花色强化购买；
- 平均计分牌数量、高低点数与人物牌使用倾向；
- 出牌次数、弃牌次数与弃牌后的成型结果；
- 基础筹码、基础倍率、独立乘区和人格效果的得分贡献；
- 已装备人格的触发次数、贡献和联动情况；
- 商店购买、刷新、金币投入和主动强化方向；
- 本局累计行为和最近三关的行为变化。

AI 只接收聚合事实和合法零件 ID，不读取完整存档，不根据自然语言反向修改战斗状态。

## 10. 图片与编号

1. 当前阶段使用预制图片资源池，不把实时生图作为人格奖励流程的前置条件。
2. 预制图片允许重复使用，以降低首版美术配置成本。
3. 图片资源 ID 随人格实例保存，刷新和读档不得重新选择图片。
4. 程序内部使用全局唯一的人格实例 ID，不使用展示编号作为存档主键。
5. 世界观和正式命名完成前，AI 人格使用顺序编号作为临时名称，例如“AI 人格001”。
6. 编号超过 099 后自然递增，不作为人格库存上限。

## 11. 仍待确认的最小事项

1. 高品质相似人格的触发相似度和效果提升幅度仍需经过数值锚定，在确认前标记为 `UNDECIDED`。

## 12. 后续开发顺序

1. 冻结动态人格实例结构、唯一 ID、临时编号和永久收藏字段。
2. 把现有 8 张基础人格拆解为可复用的条件、效果、成长和强度零件，形成首版本地白名单。
3. 建立行为聚合快照，区分本局累计数据、最近三关数据和玩家主动购买数据。
4. 建立桥接、破局、顺势三套内部匹配规则，不在正式界面暴露分类标签。
5. 建立机制指纹、全收藏查重、相似度权重和冲突检查。
6. 扩展商店人格锻造，使动态 AI 人格支持第二、第三词条生成、展示、解锁和结算。
7. 接入永久收藏、手动装备、图片资源 ID 和存档恢复；永久档只保存人格固定身份和初始主属性，活动局结束时清除第二、第三词条及全部局内成长。
8. 建立本地备用人格池，先完成完全离线的第 3、6、9 关闭环。
9. 建立 AI 请求／响应结构，让 AI 只能返回合法零件 ID、合法强度档位和表现字段。
10. 接入后端代理、超时、缓存、响应校验和错误降级。
11. 世界观完成后替换临时编号、名称、风味描述和视觉标签，不修改已锁定的规则身份。
12. 后续单独建立方案 A 的规则提案、策划审核和合法零件扩库流程。

## 13. 外部规则参考资料状态

1. 已收到 Balatro 001—150 小丑牌触发条件与效果总表，资料范围足以开始首版合法零件池拆解。
2. 该资料只作为触发时机、条件、效果、成长、资源联动和规则风险的研究输入，不直接复制牌名与数值。
3. 初步拆解结果记录在《Balatro规则参考_人格零件拆解初审.md》。
4. 当前不需要策划先手工分类 150 张规则；后续由开发侧统一完成结构化、兼容性检查和价值锚定。
