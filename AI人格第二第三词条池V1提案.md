# AI 人格第二、第三词条池 V1 提案

> 状态：`CONFIRMED`。策划已于 2026-09-03 确认本方案，现作为 AI 人格第二、第三词条的 V1 实现基线。

## 1. 体验预期

本轮只解决一个问题：AI 生成人格进入商店后，玩家能用金币继续培养它，并且每次解锁都能立刻看懂、立刻在战斗中感受到。

| 目标 | V1 预期 |
| --- | --- |
| 培养频率 | 每张 AI 人格每局最多解锁第二、第三两个词条 |
| 培养成本 | 第二词条 5 金币，第三词条 8 金币，沿用现有结构 |
| 玩家理解成本 | 只出现筹码、倍率、独立倍率三类熟悉属性 |
| 强化体感 | 第三词条约为第二词条的 2 倍，形成明确的再次投入感 |
| 生效方式 | 仅在人格主条件成功触发时附加生效，与当前 Runtime 逻辑一致 |
| 跨局规则 | 人格本体永久保留；两个词条及其解锁状态每局重置 |
| 首版范围 | 只接 AI 生成人格，不调整现有八张基础人格的 40 条词条 |

## 2. 节点拆解

| 节点 | 玩家状态 | 可执行行为 | 设计目的 |
| --- | --- | --- | --- |
| N04，第 3 战后 | 首次获得 AI 人格，构筑尚未成型 | 可花 5 金币解锁第二词条；解锁后可继续花 8 金币解锁第三词条 | 让新获得的人格马上具备可投资感，但保留金币取舍 |
| N08，第 6 战后 | 已形成初步打法 | 对新人格或旧 AI 人格进行词条培养 | 在中期提供一次明确强化选择 |
| N12，第 9 战后 | 主要打法已经稳定 | 完成人格词条，准备后四战 | 提供后期成型感，不新增陌生机制 |
| N16，第 12 战后 | 最终战前 | 补齐尚未解锁的词条 | 给此前节省金币或运气不佳的玩家最后调整空间 |

两个词条必须按顺序解锁：没有第二词条时不能直接解锁第三词条。每次解锁只随机一次，不提供反复重抽。

## 3. 数据引用与缺口

### 已确认来源

- `balance/target/shop-config.js`：人格主词条强化售价 8 金币；一次强化等价于 `+10 筹码 / +0.3 倍率 / +10% 独立倍率`。
- `balance/base-personas.js`：现有人格已经采用两个词条槽、5/8 金币、不可重复、局内重置的结构。
- `balance/target/stage-nodes.js`：前九战固定金币合计 30；商店位于 N04、N08、N12、N16。
- `persona/persona-runtime.js`：计分型副属性在人格主条件成功触发时生效；词条结果保存在人格实例中。
- 最新策划确认：AI 人格固定身份和初始主属性跨局保留，局内成长和第二、第三词条不跨局。

### 当前缺口

- 没有足够实测数据证明 `10 筹码 = 0.3 倍率 = 10% 独立倍率` 在所有阶段完全等价。
- 没有玩家在四个商店中的实际金币持有量和消费顺序分布。
- 没有 AI 人格各触发条件的真实平均触发次数。

因此 V1 直接沿用已经进入商店的价值锚点，不另造一套换算；上线测试后再用实际单手贡献修正。

## 4. 逐步推导

### 4.1 统一价值单位

现有 8 金币人格主词条强化定义为 1 个价值单位：

```text
1 价值单位 = 10 筹码 = 0.3 倍率 = 10% 独立倍率
```

### 4.2 第二词条

第二词条售价为 5 金币，按价格直接换算为：

```text
5 ÷ 8 = 0.625 价值单位
```

它最早能在 N04 获得，后续生效战斗多。为了避免前期一次随机结果过强，加入 20% 的前期安全折扣：

```text
0.625 × 0.8 = 0.5 价值单位
```

换算结果为 `+5 筹码 / +0.15 倍率 / +5% 独立倍率`。

### 4.3 第三词条

第三词条售价为 8 金币，直接按完整 1 个价值单位计算：

```text
8 ÷ 8 = 1 价值单位
```

换算结果为 `+10 筹码 / +0.3 倍率 / +10% 独立倍率`。

### 4.4 完整培养预算

```text
总花费 = 5 + 8 = 13 金币
总强度 = 0.5 + 1 = 1.5 价值单位
按价格理论值 = 13 ÷ 8 = 1.625 价值单位
实际 / 理论 = 1.5 ÷ 1.625 = 92.3%
```

完整培养比按价格硬换算低约 7.7%，作为“随机结果不可自选”和“最早 N04 即可获得”的安全余量。它依然明显强于只购买一次主词条强化，但不会把 13 金币全部等额转化为过量战力。

## 5. 推荐最终配置

### 5.1 第二词条池

| 配置 ID | 属性 | 最终数值 | 价值单位 | 基础权重 | 显示文案 |
| --- | --- | ---: | ---: | ---: | --- |
| AI_SUB2_CHIPS_050 | 基础筹码 | +5 | 0.5 | 34 | `触发时额外 +5 筹码` |
| AI_SUB2_MULT_050 | 基础倍率 | +0.15 | 0.5 | 33 | `触发时额外 +0.15 倍率` |
| AI_SUB2_XMULT_050 | 独立倍率 | +5% | 0.5 | 33 | `触发时额外 +5% 独立倍率` |

### 5.2 第三词条池

| 配置 ID | 属性 | 最终数值 | 价值单位 | 基础权重 | 显示文案 |
| --- | --- | ---: | ---: | ---: | --- |
| AI_SUB3_CHIPS_100 | 基础筹码 | +10 | 1.0 | 34 | `触发时额外 +10 筹码` |
| AI_SUB3_MULT_100 | 基础倍率 | +0.3 | 1.0 | 33 | `触发时额外 +0.3 倍率` |
| AI_SUB3_XMULT_100 | 独立倍率 | +10% | 1.0 | 33 | `触发时额外 +10% 独立倍率` |

权重 34/33/33 只用于把整数权重凑到 100，不代表筹码刻意更强。正式显示不出现权重和价值单位。

## 6. 抽取与互斥规则

1. 第二词条只能从 `AI_SUB2_*` 中抽取，第三词条只能从 `AI_SUB3_*` 中抽取。
2. 同一张人格的第二、第三词条不得出现相同属性类型。例如第二词条抽到筹码，第三词条只能在倍率和独立倍率中重新归一化抽取。
3. 副属性允许和主属性同类。主属性是人格核心路线，副属性继续强化该路线属于合理专精。
4. 抽取发生在玩家正式支付金币并确认解锁时；预览、打开商店、刷新商品均不能提前改变结果。
5. 抽取结果写入人格实例；刷新页面和读取活动局存档不能重抽。
6. 新一局重新创建人格实例时，第二、第三词条恢复未解锁。
7. AI 不选择具体数值、不返回效果代码，只能在以后需要时返回合法词条 ID；最终仍由本地校验和抽取执行。

## 7. V1 暂不加入的词条

| 类型 | 暂不加入原因 |
| --- | --- |
| 出牌次数 +1 | 会直接增加每场战斗时长、触发次数和得分上限，价值远高于普通计分词条 |
| 弃牌次数 +1 | 对不同牌型流派的价值差异较大，尚无统一价值锚点 |
| 金币获取 | 当前运行方式可能随人格触发多次，需要先增加每战/每局上限，否则会形成经济滚雪球 |
| 成长上限 +1 | 需要逐类分析不同成长规则，不适合作为通用随机词条 |
| 复制或重复触发 | 会同时放大主效果、成长效果和副属性，组合风险过高 |
| 指定牌型/花色加成 | 与现有牌型强化、花色强化商品重叠，需要先决定系统之间的职责边界 |

这些词条不是永久否决，而是留给 V2 扩池。V1 先用六条完全可解释、现有执行器已经支持的计分属性跑通闭环。

## 8. 校验结果与调参杠杆

### 当前校验

- 结构闭环：两槽、顺序解锁、不可重复、局内重置均与现有 Runtime 一致。
- 经济闭环：完整培养消耗 13 金币，相当于玩家放弃至少一次 8 金币核心强化和其他低价购买机会。
- 数值闭环：六条词条全部来自现有商店价值锚点，没有引入新效果类型。
- 理解成本：玩家只需要理解三个现有计分概念，不增加新的触发文本。
- 开发风险：无需修改计分公式，只需要增加通用 AI 词条配置和槽位限定校验。

### 首轮测试优先观察

1. N04 玩家是否愿意一次支付 13 金币完成两个词条；若过低，先调整价格，不先放大数值。
2. 三类词条的单手实际得分贡献差异；若独立倍率显著超标，第一调参项是 `10% → 5%`、`5% → 3%`。
3. 玩家是否觉得随机结果失控；若明显不满，优先改成“三选一”，不要增加免费重抽。
4. AI 人格主条件平均每战触发次数；高频人格可能需要降低副属性效果或增加每手一次限制。
5. 玩家是否更愿意培养旧人格而忽略新人格；若发生，调整商店出现方式，不直接提高 AI 人格数值。

## 9. 确认后的实现顺序

1. 新建 AI 人格通用副属性配置模块，保持与基础人格 40 条配置分离。
2. 给词条增加 `slotIndex` 限定及“相同属性类型互斥”校验。
3. 将六条词条 ID 写入 AI 人格模板，不改变现有八张人格。
4. 接通商店解锁、扣费、随机抽取、展示和战斗结算。
5. 增加 N04/N08/N12/N16 解锁测试、读档不重抽测试、下一局重置测试。
6. 通过全量回归后建立独立 Git 检查点。

## 10. 已确认的实现内容

以下四点均已确认：

1. V1 是否只做筹码、倍率、独立倍率三类，不加入金币和行动次数。
2. 第二词条是否采用 0.5 价值单位，第三词条采用 1.0 价值单位。
3. 第二、第三词条是否禁止出现相同属性类型。
4. 解锁价格是否继续保持 5 / 8 金币。

确认结论：四项全部通过。实现时只允许本地从六个合法 ID 中抽取，AI 不得直接填写数值或效果代码。
