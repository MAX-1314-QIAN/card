# AI人格多样性与整数数值扩展草案

状态：IMPLEMENTED_V1 / 已按策划确认完成首批本地接入；未部署、未推送，线上 Worker 与密钥配置未改动。

## 1. 设计摘要与体验预期

目标：任何主要打法都能获得不同的出牌目标和成长过程，而不只是换名字、换加分数值。AI继续从本地验证的候选中选择；每节点一张、不可重抽、方向不向玩家显示。

体验假设：新人格首次有效触发就能看见明确收益；仍有后续成长；每张主要说明尽量两句话。筹码用5的倍数，基础倍率优先整数，独立倍率用整数百分比。所有新增数值、分组数量、验收门槛均是待测试建议。

玩家样本分为专精牌型、专精花色、点数构筑、少牌、弃牌运营、混合探索，不采用付费分层。分别检查新手和熟练玩家，避免只有熟练者才能读懂组合。

“20种”解释：目标是每个主要打法跨多局拥有至少20个可用机制组合，不要求每个节点出现20张，也不承诺24个草案就是24种独立玩法。只换花色、牌型名、数值、上限不计新机制；改变出牌选择或收益兑现过程才计入。

## 2. 当前节点与代码依据

| 节点 | 现有安排 | 扩展设计目的 |
|---|---|---|
| N04，第三战后 | 桥接或破局 | 样本少，提供能试用的新路径，保留直接收益 |
| N08，第六战后 | 与N04交换方向 | 利用已形成的牌库与装备寻找组合，控制转型成本 |
| N12，第九战后 | 顺势 | 支持主打法，但可改变操作细节；不能要求漫长养成才有用 |

引用：balance/target/ai-persona-whitelist-v1.js、persona/ai/candidate-builder.js、persona/ai/generator.js、persona/ai/similarity.js、persona/ai/value-budget.js。

当前20类触发、4类主效果、6类成长；实际成长都是累计层数，封顶3～5层。3～5层不等于3～5手：成长频率低时会明显更慢。

当前先生成排序后的24个候选，再按历史查重并最多保留12个。淘汰后不向24个之外补货；同输入排序固定，本地兜底选第一项。中高置信度下，历史触发率低于2.5%的触发变体会被过滤。以上会使候选收敛，但实际各打法重复率尚无批量测量，不能把理论排列数当实测丰富度。

当前预算锚：10筹码≈0.3基础倍率≈10%独立倍率增量，属于现有预算近似；它不是所有牌型上的真实得分等价。主倍率和筹码当前量不同，相同加值的实际得分增幅也不同。

缺口：每节点剩余实际出牌数、各打法完整动作序列、真实AI选择分布、替换率、成长达成率。需用回放与试玩代替后文假设。

## 3. 全打法机制配置草案

以下24行为设计模板，不能直接复制进当前白名单。难度是初审：A为已有条件可望组合，B为需要新增条件或数据，C为需要新增跨手状态与提交逻辑，正式实现前仍须核实Runtime。每张仅选择一种奖励档，并绑定一种成长方案。

共同口径：牌型是最终识别牌型；花色、点数默认指计分牌；弃牌指正式弃牌动作。A/B为生成时固定、玩家可直接读到的具体目标；同牌型同花色参数替换不计机制创新。不同目标必须互异且在当前牌库中可行。

| ID | 玩家触发目标 | 适用路线 | 行为差异 | 成长绑定 | 难度 |
|---|---|---|---|---|---|
| M01 | 打出指定牌型A | 全部牌型 | 稳定专精 | G1 | A |
| M02 | 指定牌型A且恰好打出K张 | 少牌、满手 | 是否额外带杂牌 | G1 | A |
| M03 | 计分牌至少3张为指定花色 | 花色、同花 | 在其他牌型中携带目标花色 | G1 | B |
| M04 | 计分牌全部为指定点数范围 | 大牌、小牌 | 选择参与计分的点数 | G1 | B |
| M05 | 上一手A，本手B | 任意可行双牌型 | 两种牌型轮换 | G2 | C |
| M06 | 本手同花与上一手同花花色不同，且两手相邻 | 同花 | 留不同花色的备选牌 | G2 | C |
| M07 | 上一手出牌数≤2，本手≥4 | 少牌、宽出牌 | 小手铺垫大手 | G2 | C |
| M08 | 本手牌型等级高于上一手 | 混合、顺子、同点数 | 安排牌型递进 | G2 | B |
| M09 | 打出本张人格尚未记录的牌型 | 混合 | 拓展识别牌型 | G3 | B |
| M10 | 打出本张人格尚未记录的同花花色 | 同花 | 收集不同花色 | G3 | C |
| M11 | 计分牌含本张人格尚未记录的同点数组合点数 | 对子、两对、三条等 | 主动换点数；一手只计一次奖励 | G3 | C |
| M12 | 打出本张人格尚未记录最高点数的顺子 | 顺子 | 高低顺子都值得保留 | G3 | C |
| M13 | 弃牌≥2张蓄力，下一手消耗并触发 | 弃牌 | 弃牌与出牌配合 | G2 | A |
| M14 | 打出A蓄力，此后首次打B兑现；A≠B | 双牌型 | 可以跨多手等待兑现 | G2 | C |
| M15 | 连续两手没触发主奖励后蓄力，下一次打A兑现 | 专精但不稳定 | 为爆发保留目标牌 | G2 | C |
| M16 | 弃掉含指定花色的牌蓄力，下次计分牌含该花色时兑现 | 花色、弃牌 | 在弃与留之间分配花色 | G2 | C |
| M17 | 本战尚未弃牌时打出A | 保留弃牌 | 是否放弃修牌换收益 | G1 | B |
| M18 | 本战最后一次可用出牌机会打出A | 爆发、风险 | 留终结手；检测扣次数之前状态 | G1 | B |
| M19 | 本战第一次出牌打出A | 开局运营 | 是否提前弃牌整理第一手 | G1 | B |
| M20 | 剩余弃牌≥2次且打出A | 资源保留 | 花掉多少弃牌机会 | G1 | B |
| M21 | 牌型A且计分牌含指定花色 | 多种牌型、花色 | 牌型与花色同时满足 | G1 | B |
| M22 | 计分牌至少含3种花色 | 混合、顺子 | 分散花色 | G1 | A |
| M23 | 计分牌点数全部为偶数 | 点数、同点数结构 | 建立偶数牌库；只允许2/4/6/8/10/Q | G1 | B |
| M24 | 本手计分牌至少一个点数与上一手计分牌重复 | 点数、混合 | 保留能延续点数的牌 | G2 | C |

M01是必要的简单基线，不是新机制；M09～M12是同一收集家族的不同操作目标。M23不能与五张连续点数顺子绑定；牌型与张数矛盾、同花与三花色矛盾等组合直接剔除。皇家同花顺等低频牌型不能仅因被玩家打过一次就判为可持续专精。

机制家族固定映射如下，只用于生成器内部做覆盖与查重，前端不显示“顺势/桥接/破局”或家族标签：

| 家族 | 模板 | 主要提供的新鲜感 |
|---|---|---|
| 专精 | M01～M04 | 同一主打法下改变出牌张数、花色和点数选择 |
| 交替 | M05～M08 | 让前后两手形成顺序，而非一直重复最强牌型 |
| 收集 | M09～M12 | 本局逐步发现不同牌型、花色、组合点数或顺子高度 |
| 蓄力 | M13～M16 | 先准备、后兑现，形成明确的小爆发 |
| 资源 | M17～M20 | 用弃牌和出牌时机换取收益 |
| 组合 | M21～M24 | 把牌型、花色、奇偶和前手点数组合起来 |

成长方案：

- G1：正式触发累计，每达到配置次数增强一次，达到成长上限后主奖励继续触发。
- G2：每成功完成一次交替/兑现累计一次成长；蓄力上限1，不能在兑现同一手又蓄力；战斗结束清除蓄力，局内成长保留。
- G3：获得后记录新目标，一手可以记录多个新点数但最多推进一次成长。集齐时新增一次终结奖励并转为“满足对应结构就触发”；终结奖励数值UNDECIDED，必须从同一预算分配，禁止成长结束后变废牌。

统一：成长本手提交后生效，从下一手起增加奖励；预览不成长、不消费蓄力。未装备时不成长、不推进人格专属记录；换下再装回保留本局进度。跨局清空成长/蓄力/记录，永久只保留生成时的规则及初始强度。第二第三词条继续按现有局内解锁方式，成本本轮不改。

M18/M19等每战低频规则不能照搬“触发6次才成长”，需要按剩余战斗次数校准。N12优先直接收益足够、剩余流程至少有机会成长一次的配置。

## 4. 候选流程与多样性

建议流程：合法组合枚举 → 当前牌库可行性 → 数值预算 → 机制历史查重 → 按机制家族抽样 → DeepSeek选一张 → 本地复验 → 写存档。

建议仍最多12张给DeepSeek，优先每个家族2张、覆盖6家族；不足时从其他可用家族补足，每家族初始最多3张。可行性与硬查重优先，不能为凑12个放入废牌。每个家族内按适配度加权抽样，避免固定取最高分；抽样结果在首次节点请求前保存，存档恢复复用候选及最终结果。

历史分为规则完整指纹和玩法指纹。完整指纹识别完全相同配置；玩法指纹忽略名字、基础数值和成长上限，但保留触发对象、动作顺序、成长与消耗方式。换数值不算新玩法。

已装备和本局获得优先硬排除近似；永久收藏继续查重。最近3局见过的家族建议降权，但不能硬禁整个家族导致池枯竭。新增跨局“见过”历史须版本迁移：旧档记为空，不猜测历史；既有收藏硬查重继续有效。

破局可使用牌库可行、历史未使用的机制；以当前牌库抽样估计触发机会，不能把历史0次直接当永远不可行。顺势保留主路线，同时允许改变出牌顺序、花色或成长目标，不必总选M01。

先制作机制覆盖审计：6个打法样本 × 3个节点 × 多组装备收藏历史。要达到“每打法20种以上”，必须实际筛选后统计，不以24模板乘牌型数报喜；不足的格子再补设计。

## 5. 数值逐步推导

体验目标先定为同类人格期望贡献小幅提升，初版预算上限建议增加20%；这是调参假设，不是所有牌固定提高20%。先调初始可感知收益，成长和副属性总量一并检查。

沿用旧锚仅做第一道过滤：

U(筹码)=筹码/10；U(基础倍率)=倍率/0.3；U(独立倍率增量)=百分比/10。

初始每手期望=p×U(初始奖励)。
成熟每手期望=p×U(初始奖励+累计成长)。
预计成长所需出牌数=成长所需事件数/每手成长事件率。

| 节点 | 旧初始/成熟预算 | ×1.2后的建议上限 |
|---|---|---|
| N04 | 0.9 / 1.8 | 1.08 / 2.16 |
| N08 | 1 / 2 | 1.2 / 2.4 |
| N12 | 1.1 / 2.2 | 1.32 / 2.64 |

内部预算可带小数；玩家看到的奖励取整。p需要统计或模拟；组合条件不能用最小边际概率当作准确联合概率。带蓄力的p是兑现频率，而非蓄力频率。用完整动作回放估计成长时间与实际收益。

取整反例：+0.3倍率直接改成+1是增加233%，不是20%。若原触发率60%，期望0.18倍率/手；希望提高20%到0.216，用整数+1须约21.6%触发率，不能让60%的原条件仍每次+1。应换奖励类型，或设计明确累计兑现；不能暗中漏触发。

## 6. 候选数值档（建议配置，不是统一套用）

以下三个N12算例仅验证预算可行性，概率为假设；其增长节奏不同。三种效果不是绝对等价。

| 档ID | 假设触发率 | 初始显示 | 成长 | 成熟显示 | 初始/成熟期望单位 | 结论 |
|---|---|---|---|---|---|---|
| NUM_C | 60% | +20筹码 | 每触发2次，奖励+5筹码；最多2次 | +30筹码 | 1.2 / 1.8 | N08/N12预算内；预计6.7手成长满 |
| NUM_M | 30% | +1基础倍率 | 累计触发3次后，奖励永久+1倍率，本局仅一次 | +2基础倍率 | 1 / 2 | 三节点预算内；预计10手，N12可能太慢需另筛 |
| NUM_X | 60% | +20%独立倍率 | 每触发一次，奖励+5个百分点；最多4次 | +40%独立倍率 | 1.2 / 2.4 | N08/N12预算内；预计6.7手成长满 |

解释：NUM_X从20%成长到25%，不是20%乘1.05。实际如何与现有其他独立乘区合并，必须保持运行时原运算顺序。

建议奖励粒度：筹码5；基础倍率1；独立倍率5个百分点。先按目标触发率分配初始与成长预算，再选粒度取整，最后重新验预算；超预算则调整档位或改用其他效果，不以“取整”为由超额放行。

不把×1.2改写为×2。若乘法显示改为“本牌使得分提高20%”，需限定这是单牌乘法作用，不能与加法独立倍率混淆；正式文案仍需逐类核对。

不建议本轮统一把所有成熟数值再翻倍。多机制、商店主强化、两条副属性与四槽联动可能相乘，必须检查组合结果。现有商店+0.3倍率本轮保留，不能为了显示整数把商店收益四舍五入；全面整数化是另一个需批准的数值迁移。

### 6.1 六档整数奖励模板

为避免24个机制各写一套散乱数值，建议先只保留六个奖励模板。表中触发率是配置筛选目标，不是已经测得的数据；生成时若行为快照和模拟牌库不在对应区间，该机制不能套用此档。

| 档位 | 目标触发率 | N04初始→成熟 | N08初始→成熟 | N12初始→成熟 | 成长节奏 |
|---|---:|---:|---:|---:|---|
| C50 高频筹码 | 45%～55% | 20→30筹码 | 20→35筹码 | 25→40筹码 | 每正式触发2次+5筹码 |
| C40 中频筹码 | 35%～45% | 25→40筹码 | 30→45筹码 | 30→50筹码 | 每正式触发2次+5筹码 |
| C25 低频筹码 | 20%～30% | 40→60筹码 | 45→70筹码 | 50→80筹码 | 每正式触发1次+10筹码 |
| M30 低频基础倍率 | 25%～32% | 1→2倍率 | 1→2倍率 | 1→2倍率 | N04/N08累计3次、N12累计2次后+1倍率，仅成长1次 |
| X50 高频独立倍率 | 45%～55% | 20%→35% | 20%→40% | 25%→45% | 每正式触发2次+5个百分点 |
| X30 低频独立倍率 | 25%～32% | 30%→50% | 35%→55% | 40%→60% | 每正式触发1次+10个百分点 |

取整过程示例：N12的C50按中位触发率50%计算，初始期望单位为`0.5×25/10=1.25`，低于N12建议上限1.32；成熟期望单位为`0.5×40/10=2.00`，低于成熟上限2.64。保留0.64单位作为多槽组合、概率误差和商店强化缓冲，不把预算全部吃满。

M30无法做“每关都更高”的整数初始值：+2基础倍率在30%触发率下初始期望单位约为2，明显超过初始预算。因此三个节点先保持+1，用成长次数和触发条件体现后期强度。若试玩认为+1仍不够爽，应先降低触发频率并改成兑现型大数字，不能直接把所有倍率改成+2。

### 6.2 24模板配置映射

这是策划审核表，不是正式白名单。`首批`表示可优先进入V1开发审查；`后续`表示需要跨手状态或更复杂记录。N04只允许桥接/破局池，N08使用另一种桥接/破局池，N12使用顺势池；标签只存在内部配置，不展示给玩家。

| 机制ID | 家族 | 建议奖励档 | 节点用途 | 批次 | 配置备注 |
|---|---|---|---|---|---|
| M01 | 专精 | C50 | N04桥接、N08破局、N12顺势 | 首批 | A从玩家可持续牌型中选；低频牌型改C25 |
| M02 | 专精 | C40 | N04桥接、N08破局 | 首批 | K必须与A的计分张数相容 |
| M03 | 专精 | C50 | N04桥接、N12顺势 | 首批 | 目标花色须在当前牌库有足够密度 |
| M04 | 专精 | C40 | N04破局、N08桥接 | 首批 | 点数范围只用“大牌/小牌”两套固定定义 |
| M05 | 交替 | M30 | N08桥接、N12顺势 | 后续 | A/B均需在模拟中可连续完成 |
| M06 | 交替 | X30 | N08破局、N12顺势 | 后续 | 至少两种花色具备可成同花密度 |
| M07 | 交替 | M30 | N04破局、N08桥接 | 后续 | 小手铺垫不要求小手成功得分 |
| M08 | 交替 | M30 | N08桥接、N12顺势 | 首批 | 使用固定牌型等级表，平级不触发 |
| M09 | 收集 | C50 | N04桥接、N08破局 | 后续 | 必须按人格实例记录，不能借用全局牌型历史 |
| M10 | 收集 | X30 | N08桥接、N12顺势 | 后续 | 每种同花花色仅记录一次 |
| M11 | 收集 | C40 | N08桥接、N12顺势 | 后续 | 同一手多个对子只推进一次成长 |
| M12 | 收集 | X30 | N08破局、N12顺势 | 后续 | 只在顺子最高点数首次出现时触发 |
| M13 | 蓄力 | M30 | N04破局、N08桥接 | 首批 | 蓄力上限1，下一次正式出牌必消耗 |
| M14 | 蓄力 | X30 | N08桥接、N12顺势 | 后续 | A≠B；兑现失败不会重复蓄力 |
| M15 | 蓄力 | X30 | N08破局、N12顺势 | 后续 | 连续未触发计数在战斗开始时清零 |
| M16 | 蓄力 | M30 | N04桥接、N08破局 | 后续 | 弃牌花色与后续计分花色一致才兑现 |
| M17 | 资源 | C40 | N04破局、N08桥接 | 首批 | 只读“本战是否弃过牌”，不扣资源 |
| M18 | 资源 | X30 | N08破局、N12顺势 | 首批 | 每战最多一次，按低频档校准 |
| M19 | 资源 | C40 | N04桥接、N08破局 | 首批 | 每战最多一次；允许先弃牌再出首手 |
| M20 | 资源 | C40 | N04桥接、N08破局 | 首批 | 读取提交前剩余弃牌次数 |
| M21 | 组合 | C25 | N08桥接、N12顺势 | 首批 | 牌型A与花色必须同时可行 |
| M22 | 组合 | C50 | N04破局、N08桥接 | 首批 | 同花与该机制互斥 |
| M23 | 组合 | C40 | N08破局、N12顺势 | 后续 | Q计为偶数，A/J/K不计；与五张顺子互斥 |
| M24 | 组合 | M30 | N08桥接、N12顺势 | 后续 | 只比较实际计分牌，不比较全部出牌 |

首批推荐12个：M01、M02、M03、M04、M08、M13、M17、M18、M19、M20、M21、M22。M09调整到后续批次，因为当前`UNIQUE_HAND_TYPE_FIRST_TIME_THIS_RUN`读取的是全局历史，不符合“从获得本张人格后开始记录”的设计语义。若Runtime核查发现其中一个条件不可安全落地，优先用同家族的可行模板替补，不在`game.js`增加人格名称特判。

### 6.3 “每个打法20种”的实际组成

20种是跨N04/N08/N12、顺势/桥接/破局和机制参数形成的可玩候选总数，不要求一次请求展示20张。生成器每次仍最多给DeepSeek 12张，最终玩家只看到1张。

以“两对”为例，至少应能来自：两对专精、两对且恰好4张、指定花色参与两对、大小点数限制、牌型交替、牌型递进、新对子点数收集、弃牌蓄力、无弃牌两对、首手两对、保留弃牌两对、两对+花色、偶数两对、延续上一手点数等机制。把目标花色从红桃换成黑桃，只是参数变化，不单独算一个新机制。

顺子、同花、高牌、对子系和混合路线必须各自制作同样的覆盖审计。若某路线只能靠更换花色或数值达到20条，该路线仍判定不合格，继续补机制，而不是放宽统计口径。

## 7. 经济与成长边界

本轮不新增金币、抽牌、复制人格、免费刷新等直接资源产出。M17～M20仅读取资源或动作状态，奖励仍为评分属性，因此没有新增直接金币来源；但更快击败关卡可能间接提高剩余行动结算金币，必须测量。

人格主强化、两条副属性价格不动；测试购买前后得分贡献，避免新主属性强到副词条失去价值，也避免必须买满才可用。永久收藏不保存局内强化。已生成旧人格保留原始规则数值，新生成才用新版本；不能把已收藏0.45强制舍入。新增机制/历史结构需要独立版本与迁移测试。

## 8. 验收与调参顺序

以下为建议门槛，不是测试通过结论：

1. 每主要打法跨节点累计至少20个可行玩法组合；只换数值与目标名称不重复计数。逐格列不足，不以总体总量替代。
2. 单批候选优先覆盖6家族，达不到时记录是可行性、查重还是预算所致；不隐藏候选不足。
3. 多局重复测试统计最近3局同机制重现率、DeepSeek最终选择家族分布、本地兜底分布；随机候选不会保证AI最终均匀。
4. 对照同种子同操作回放，测新增人格初始/成熟得分、成长首次发生手数、失败率与结算金币；先看各路线再看平均。
5. 测最强四槽+商店主强化+全部副词条；测蓄力同手消费、跨战清除、预览无副作用、刷新不重抽、永久带出还原初始值。
6. 新手只看卡面能说明“如何触发、什么时候变强”，熟练玩家能指出至少一个有意义的操作变化。描述复杂但不改变决策的模板删掉。

调参优先级：机制覆盖 → 不可行候选 → 近期重复权重 → 初始收益 → 成长速度 → 成熟上限。不要先靠提高DeepSeek随机温度补内容，也不要一开始就加大单次请求候选数。

## 9. 推荐开发顺序与待确认项

阶段一：候选先查重再补齐、机制家族抽样、跨局见过记录，维持原机制；验证候选收敛是否改善。

阶段二：先评审并接入A类及少量B类，扩大花色/点数与牌型组合；执行整数奖励预算迁移，只影响新人格。

阶段三：加入交替、收集、蓄力兑现通用状态能力，按机制族做Runtime测试，避免在game.js逐张写特判。

建议优先审核：20%预算增幅；5/1/5个百分点的奖励粒度；24模板中首批实现范围；收集毕业奖励（UNDECIDED）。批准前不改正式数值，不部署。

## 10. 首批12机制Runtime兼容性核查

核查依据：`persona-condition-evaluator.js`、`persona-effect-executor.js`、`persona-runtime.js`、`battle/score-runtime.js`、`behavior-snapshot.js`、`candidate-builder.js`、`similarity.js`、`generator.js`、配置校验器与现有AI人格测试。

结论：首批12个中，4个可以主要通过白名单声明完成，7个需要增加通用条件与行为统计，1个需要补全局前手历史。没有任何一个需要在`game.js`按人格名称写特判。M09已移出首批。

| 机制 | 当前能力 | 核查结论 | 需要补充 |
|---|---|---|---|
| M01 指定牌型 | `HAND_TYPE_IS`及主/次牌型动态来源已存在 | 配置可用 | 新增机制家族与整数奖励档引用 |
| M02 牌型+恰好K张 | `HAND_TYPE_IS`、`SUBMITTED_CARD_COUNT_EXACT`、多条件AND均已存在 | 配置可用 | 增加牌型与K的合法组合表；联合触发率先保守估计 |
| M03 至少3张指定计分花色 | 计分牌已进入Runtime，但无花色计数条件 | 需通用条件 | `SCORING_SUIT_COUNT_AT_LEAST`及行为快照对应统计 |
| M04 计分牌均属指定点数段 | 计分牌已有`r/ri`，行为层已有low/middle/face/ace划分 | 需通用条件 | `ALL_SCORING_CARDS_IN_RANK_BAND`；复用统一点数段定义，不再写第二套口径 |
| M08 本手牌型等级高于上一手 | 当前只有上一手牌型，无上一手等级 | 需历史扩展 | `previousHandPriority`与`HAND_PRIORITY_HIGHER_THAN_PREVIOUS`，战斗开始清空 |
| M13 弃至少2张后下一手 | 通用Flag、弃牌事件、设置/消费Flag均已存在 | 配置可用 | 新增阈值2变体；保持Preview不消费、Commit才消费 |
| M17 本战从未弃牌 | 通用Battle Flag和弃牌事件已存在 | 配置可用 | 新建`discardedThisBattle`，弃牌后置true，出牌后不清除 |
| M18 最后一次出牌 | `remainingHands`已在扣除前传入Runtime | 需通用条件 | `REMAINING_HANDS_EXACT=1`；每战最多触发一次 |
| M19 本战第一次出牌 | `handIndex`已传入Runtime | 需通用条件 | `HAND_INDEX_EXACT=1` |
| M20 至少保留2次弃牌 | `remainingDiscards`已传入Runtime | 需通用条件 | `REMAINING_DISCARDS_AT_LEAST=2`，按提交前数值判断 |
| M21 牌型+指定计分花色 | 多条件AND已存在，缺计分花色条件 | 需通用条件 | 复用M03条件；增加不可行组合过滤 |
| M22 计分牌至少3种花色 | 当前`MIN_UNIQUE_SUITS`统计全部出牌，不是计分牌 | 需通用条件 | 新增`scoringUniqueSuitCount`与`MIN_SCORING_UNIQUE_SUITS`；不改变旧条件语义 |

### 10.1 已发现的结构问题

1. 当前生成器先从全部合法组合中截取24个，再做相似度排除，排除后不会从剩余合法组合补位。这会直接造成“明明池很大，最终还是只有几张”的体验。应改成：保留完整合法候选索引，查重后按家族补足最多12张。
2. 当前候选分配只限制触发零件、效果和成长零件次数，没有“六个机制家族”概念。需要在候选元数据加入`mechanismFamilyId`，按家族先取、再补位；该字段不进入玩家界面。
3. 当前组合条件触发率使用边际概率的最小值，并不是真实联合概率。这个做法通常会高估触发率，因此数值上偏保守、体验上可能偏弱。首批可继续作为安全下限，但行为快照需要补组合命中统计用于后续校准。
4. 当前永久收藏记录没有`runsSinceLastUsed`，相似度模块虽然读取该字段，实际通常拿到`null`，所以“三局未使用后允许高品质相似版本”尚未真正闭环。此项可与跨局近期机制降权一起单独升级收藏版本，不能在本批偷偷补规则。
5. 当前`MIN_UNIQUE_SUITS`按全部提交牌统计。新机制必须另增“计分牌花色”条件，不能修改旧条件含义，否则现有基础人格、白名单候选和测试会一起改变。

### 10.2 建议文件边界

| 文件/模块 | 只负责什么 | 禁止什么 |
|---|---|---|
| `balance/target/ai-persona-whitelist-v1.js` | 机制、整数档、适用方向、合法参数 | 不写执行函数，不访问DOM |
| `persona/persona-condition-evaluator.js` | 纯条件判断 | 不改状态，不决定奖励数值 |
| `persona/persona-runtime.js` | Preview/Commit、历史与作用域状态 | 不按人格ID或名称特判 |
| `battle/score-runtime.js` | 组装计分上下文 | 不决定AI候选和人格成长策略 |
| `persona/ai/behavior-snapshot.js` | 统计玩家行为与数据质量 | 不输出可执行规则 |
| `persona/ai/candidate-builder.js` | 合法组合、触发率、预算、家族初筛 | 不直接调用DeepSeek |
| `persona/ai/similarity.js` | 当前局、永久收藏和近期机制查重 | 不改变候选数值 |
| `persona/ai/generator.js` | 查重后补位与最终本地兜底 | 不绕过本地校验 |
| `balance/schema-validation.js`与白名单校验器 | 新条件、字段、档位的结构验证 | 不自动修复错误配置 |

### 10.3 开发验收清单

首批接入必须新增以下自动测试，全部通过后才允许进入正式生成池：

- 12个机制分别测试通过/不通过边界，尤其是计分牌与提交牌的区别。
- 每个新条件验证Preview不改状态，Commit只执行一次，刷新读档结果一致。
- M08跨手有效、跨战清空；M13蓄力存档恢复、预览不消费、提交消费；M17出牌后不清除弃牌历史。
- 六个奖励档分别复算N04/N08/N12初始与成熟预算，取整后仍不超预算。
- 对子系、顺子、同花、高牌、混合、弃牌六套行为样本，查重后候选家族覆盖与补位正确。
- DeepSeek返回合法ID、非法ID、超时、空响应四种情况均只能落到本地合法候选。
- N04/N08/N12首次生成、刷新恢复、装备替换、不装备、永久带出与下一局成长清空保持现有流程。

### 10.4 实际实施顺序

1. 先修候选“查重后不补位”和家族抽样，这是新鲜感收益最高、且不改变战斗结算的改动。
2. 接入M01、M02、M13、M17四个声明式机制，验证整数奖励模板和家族调度。
3. 新增M03、M04、M18、M19、M20、M21、M22的通用条件与行为统计。
4. 扩展上一手等级历史并接入M08。
5. 完成六打法覆盖审计和同种子数值回放，再决定是否把首批机制加入正式池。
6. M09及其余跨手收集、交替、蓄力机制进入下一批，不与首批状态改造同时开发。

## 11. 首批实施结果（2026-09-06）

本轮已正式接入 M01、M02、M03、M04、M08、M13、M17、M18、M19、M20、M21、M22，共12种机制。M09仍按原计划延期，因为它需要“每张人格独立记录本局新牌型”，不能误用当前全局牌型历史。

完成内容：

- 白名单由20个触发零件扩展为30个，并增加专精、交替、收集、蓄力、资源、组合六个隐藏机制家族；家族只参与生成调度，不显示在玩家界面。
- 候选构建先扩大到最多96个合法候选，再执行历史查重；被过滤后会从后续候选补足，不再固定卡在最前24个。
- 候选优先覆盖不同触发与机制家族，再按效果和成长限制补位，减少同一打法连续获得近似人格。
- 新增计分花色、点数区间、上一手牌型等级、剩余出牌/弃牌、计分牌花色种类等通用条件，并补齐行为快照数据。
- 玩家可见数值已采用整数口径：筹码为整数，基础倍率为整数，独立乘区显示为整数百分比；基础倍率成长采用 `+1`，封顶后显示 `+2`。
- N04、N08、N12的生成预算在原基线基础上约提高20%，仍由本地预算器与白名单共同限制，DeepSeek只能选择合法候选ID。
- 旧存档没有上一手牌型历史时自动补默认值；已落盘的人格模板不被重新生成。

验证结果：根目录67个自动测试全部通过；Cloudflare Worker候选选择与越权防护5个测试全部通过；`git diff --check`通过。新增专项测试覆盖新条件边界、Preview/Commit状态、跨战清理、整数展示、六家族分布和查重后补位。

本轮没有修改线上 Worker、Cloudflare变量、生产 API Key、人格卡槽界面或玩家可见的“顺势/桥接/破局”标签，也没有提交或推送到GitHub。
