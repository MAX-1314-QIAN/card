# 《人格牌》AI 接入与玩法兼容规则规格

## 1. 功能定位

1. AI 在《人格牌》中承担“镜中观察者”和“人格解释者”的职责，不承担裁判、数值设计器或随机数生成器职责。
2. AI 的价值是把玩家已经发生的出牌、弃牌、牌型、资源使用和构筑变化转化为有依据的台词、总结、人格命名与推荐理由。
3. 断网、超时、模型停服或返回非法内容时，玩家仍必须能够完成战斗、结算、商店、人格成长、局终报告和永久收藏。
4. 同一份活动局存档无论是否启用 AI，其牌序、节点、目标分、商店结果、人格效果、奖励和胜负结果必须保持一致。

## 2. 权威边界

1. 牌型判定、计分顺序、目标分、出牌次数、弃牌次数、金币、商店刷新、人格触发、Boss 规则、节点推进、D20、随机结果、奖励和胜负全部由本地规则层决定。
2. AI 不得返回自由数值、新牌型、新效果、新触发条件、新商品、新奖励或可执行规则。
3. AI 不得直接修改活动局状态，也不得作为战斗、商店、存档或结算模块的前置依赖。
4. AI 可以对本地提供的合法标签和人格模板给出排序或匹配权重；本地必须重新校验 ID、范围和完整性，并保留最终选择权。
5. 任何来自 AI 的自然语言都只能保存为表现字段，不得再被解析为战斗规则。

## 3. 接入层级

### 3.1 离线基础层

1. 本地行为系统计算固定指标、事实证据、标签、人格模板匹配分和回退文案。
2. 离线基础层必须覆盖全部正式流程，是联网 AI 的完整替代方案。
3. 每一种 AI 输出都必须拥有对应的本地文案模板，不得出现“AI 不可用，因此无法继续”的流程。

### 3.2 AI 表现增强层

1. 首次正式接入只启用表现增强层。
2. 表现增强层生成人格名称、局终总结、推荐理由、Boss 阶段台词和卡牌表层名称。
3. 表现增强层不改变候选人格的模板、效果、品质、数值、出现概率和收藏结果。
4. AI 返回前，界面先展示本地结果；AI 合法返回后，只替换对应表现文案，不回滚或重播规则结算。

### 3.3 受限导演层

1. 受限导演层不在首次接入版本启用。
2. 启用后，AI 只能在本地提供的合法 Boss 台词主题、叙事主题、标签 ID 或人格模板 ID 中给出权重建议。
3. 本地根据白名单、冲突规则、保底规则、去重规则和确定性随机完成最终选择。
4. AI 权重缺失、越界、重复或不合法时，整组建议失效，系统使用本地匹配结果。
5. AI 不得根据玩家表现实时提高目标分、改变牌序、克制当前牌库或暗改商品池。

## 4. 玩法接入位置

### 4.1 战斗前观察

1. 进入战斗节点时，本地先锁定该节点的 Boss、规则、目标分、介入内容和随机种子。
2. AI 只读取进入节点前已经形成的聚合行为快照，为当前 Boss 生成观察台词。
3. 台词可以指出玩家近期偏好的牌型、弃牌倾向或资源使用方式，但不得宣称 Boss 因此修改了规则。
4. AI 未及时返回时，直接播放本地阶段台词，战斗不等待 AI。

### 4.2 战斗过程

1. 战斗中不逐手请求 AI。
2. 出牌反馈、人格触发、计分动画和 Boss 反应使用本地事件与预生成台词。
3. AI 台词只解释已经完成的事件，不预告未锁定的随机结果，不影响玩家可操作时间。
4. 同一局内已经展示过的 AI 台词必须写入节点运行状态；读取存档时直接恢复，不重新请求和重抽。

### 4.3 商店与人格成长

1. 商品、价格、刷新、购买限制、副词条池、解锁结果和金币扣除全部由本地完成。
2. AI 可以解释某个商品或人格属性与玩家当前构筑的关系，但不得把解释包装成额外效果。
3. AI 推荐必须明确表达“更符合本局倾向”，不得表达为“数值最强”或“必选”。
4. 玩家购买、解锁或离开商店均不等待 AI 文案生成。

### 4.4 局终人格报告

1. 本地先计算行为指标、事实证据、合法标签、人格候选、D20 档位、候选去重和匹配分。
2. AI 根据本地事实生成人格名称、总结和三张候选的推荐理由。
3. AI 只能引用输入中存在的行为事实，不得虚构玩家没有发生的行为。
4. 玩家选择和永久带出的人格模板由本地候选决定；AI 名称与解释只作为该收藏记录的表现快照。
5. AI 不可用时，使用本地名称与总结，永久收藏流程照常完成。

## 5. 请求与响应规则

1. 客户端只向团队控制的后端代理发送请求，生产 API Key 不得进入网页、Unity 客户端、配置文件或存档。
2. 请求只包含匿名请求 ID、规则版本、语言、场景类型、节点信息、聚合行为指标、事实证据摘要、合法标签 ID 和合法模板 ID。
3. 请求不得包含账号凭据、完整本地路径、设备敏感信息、逐帧鼠标轨迹或与玩法无关的个人信息。
4. 响应必须符合固定版本结构；客户端依次校验结构版本、字段类型、ID 白名单、长度、内容安全和事实引用。
5. 任一关键字段非法时，整包响应失效，不混用部分 AI 内容与部分本地规则结果。
6. 单次请求超过八秒未完成即视为失败；当前页面不无限重试。
7. 后端代理负责模型供应方切换、限流、超时、内容过滤、匿名日志和成本控制，客户端不依赖具体模型名称。

## 6. 存档与恢复兼容

1. 存档只保存本地权威状态、已经采用的表现文本、文本来源、本地回退 ID、请求结构版本和内容版本。
2. 存档不得保存 API Key、完整 Prompt、供应方内部参数或模型上下文。
3. 节点首次进入与存档恢复必须分离；恢复时不得重新请求已经采用的台词、总结或人格名称。
4. AI 请求进行中发生退出时，存档保留本地回退结果；恢复后继续使用该结果，不阻断活动局。
5. AI 文案结构升级时，旧存档继续显示旧文本；不得为了刷新文案改变旧存档的规则结果。

## 7. 内容安全与叙事边界

1. AI 只能评价本局玩法行为，不得把行为指标描述为玩家现实中的人格、智力、疾病、心理状态或道德品质。
2. Boss 可以挑衅玩家的牌桌选择，但不得进行现实身份攻击、歧视、羞辱或诱导付费。
3. 人格名称和总结必须保持游戏世界观表达，并能追溯到至少一项本地事实证据。
4. 被内容过滤器拦截的响应直接降级为本地文案，不向玩家展示原始非法内容。

## 8. 一致性与异常处理

1. 同一规则版本和同一随机种子下，启用或关闭 AI 后的权威玩法结果必须完全一致。
2. 重复请求使用同一个匿名请求 ID；后端返回缓存结果，防止重复计费和同一节点文案反复变化。
3. AI 返回晚于页面关闭、节点切换或活动局结束时，客户端丢弃该响应，不写入其他节点。
4. 服务端返回未知模板 ID、未知标签 ID、自由数值、超长文本或结构不匹配时，客户端记录匿名错误码并使用本地回退。
5. AI 服务异常不得触发奖励补发、节点回滚、重复收藏或重新随机。

## 9. 开发顺序

1. 先冻结本地行为快照、事实证据、标签、人格模板匹配和回退文案，确保完全离线闭环。
2. 建立版本化请求与响应结构、后端代理、客户端校验器、超时和缓存。
3. 首先接入局终人格名称、总结和推荐理由，以最低风险验证完整链路。
4. 其次接入战斗前 Boss 观察台词，并继续禁止逐手联网请求。
5. 完成断网、超时、非法响应、重复请求、跨节点迟到响应、存档恢复和模型切换测试后，才允许进入正式版本。
6. 受限导演层保持关闭，直至 Boss 正式规则池、冲突过滤、人格模板池和确定性随机规则全部稳定并获得策划确认。

## 10. 验收规则

1. 关闭网络后能够从开始游戏完整运行至局终收藏，不出现流程缺失。
2. 对同一活动局分别启用和关闭 AI，牌序、计分、商店、节点、奖励、人格效果和最终胜负完全一致。
3. AI 返回非法 ID、自由数值、超长文本、诊断性内容或错误结构时，客户端全部拒绝并使用本地结果。
4. 战斗过程中不存在阻塞式 AI 请求，出牌与结算不受网络延迟影响。
5. 读取存档后，已经锁定的规则、随机结果和已采用文案不发生变化。
6. 发布构建和存档中不存在生产 API Key、完整 Prompt、个人路径或模型供应方凭据。
