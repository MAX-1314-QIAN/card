# AI 人格正式启用与后端接入边界

> 状态：`REMOTE_V1_DEPLOYED_SMOKE_TESTED`。公网 Cloudflare Worker 已部署，DeepSeek Secret 与本地开发来源已配置；2026-09-04 完成真实 HTTP 200 联调。正式局仍坚持“本地产生并校验合法候选，远程 AI 只选择候选 ID”，远程异常时自动回退本地第一候选。

## 玩家流程

- N04、N08、N12 各生成一张人格牌。
- 新局使用 `LOCAL_V1` 候选生成：本地先根据玩家行为生成并校验合法候选；网络可用时由远程 AI 从合法候选中选择一张，网络异常时使用本地第一候选。
- 旧活动局若没有生成模式字段，恢复为 `LEGACY_PROTOTYPE`，本局继续沿用旧原型人格。
- 已经生成的人格、方向和行为快照全部写入节点存档，刷新或读档不重抽。

## 后端以后能做什么

后端只接收：

- 通过隐私校验的行为统计快照；
- 当前节点和内部方向；
- 本地已经验证通过的候选 ID 与简明描述。

后端只允许返回：

```json
{
  "requestId": "与请求完全一致",
  "selectedCandidateId": "本次候选列表中的一个 ID"
}
```

后端不能返回新规则、自由数值、效果代码、胜负结果或完整人格对象。返回陌生 ID、过期 `requestId`、错误数据类型、超时或无响应时，客户端自动使用本地第一候选。

## 已建立的联网边界

- `game/ai-persona-selection-client.js`：网页端可选请求、6.5 秒超时和无网络降级。
- `game/ai-persona-api-config.js`：只保存公开 Worker 接口地址；禁止保存 Key。
- `cloudflare/ai-persona-worker/`：校验来源、请求大小、隐私字段、节点、候选和模型返回。
- Worker 只调用 DeepSeek 选择候选 ID；服务异常返回 503，客户端立即使用本地第一候选。
- 联网成功、联网失败原因和最终选择来源写入节点运行数据，已生成实例继续遵循读档不重抽。

## 已完成的远程联调

- Worker：`https://persona-card-ai-selector.dibajipa55.workers.dev`
- 接口：`/api/ai-persona/select`
- 本地冒烟测试已返回 HTTP 200，并只返回请求中的合法候选 ID。
- DeepSeek API Key 仅保存在 Cloudflare Secret，客户端配置中没有密钥。

## 下一阶段剩余内容

- 完整正常游玩评估人格与打法的匹配质量；
- GitHub Pages 正式来源 `https://max-1314-qian.github.io` 已写入仓库部署配置；Cloudflare 线上变量和 Worker 代码仍需完成一次发布后才会生效；
- 配置生产限流、监控和异常告警；
- 待世界观确定后接入正式名字、描述和专属图片。

生产 API Key 不得放进客户端。联网前必须由服务器代为调用模型。

## 2026-09-05 游戏内联调记录

- 测试来源：`http://localhost:8080`，与 `127.0.0.1` 的浏览器存储隔离。
- 第一关实际出牌，后续使用现有开发者工具推进；本次只验证功能流程，不作强度、时长或行为匹配质量结论。
- N04、N08、N12 均在开发面板显示 `REMOTE_RESPONSE / AI_SELECTED`。
- 三次获得窗口分别刷新并继续游戏，窗口文字与刷新前完全一致。
- N04 替换基础人格，N08 保留装备，N12 替换此前生成的人格，均能正常进入后续商店。
- 最终报告包含三张 AI 人格，带出窗口正确提供三选一及副属性不保留提示。
- 用户确认后已永久带出 AI人格003；刷新图鉴仍保留该牌，未选择的 AI人格001、002 未进入收藏。
- 后续浏览器续测已完成：新局拖动 AI人格003 到第一槽，确认阵容并开始战斗；战斗左侧显示原始触发条件“对子或葫芦”、主属性“+0.45 倍率”，两条副属性均未解锁。永久带出、刷新保留、下一局装备的浏览器流程验证通过。
- 开发工具补充各节点请求状态和选择来源，仅开发模式可见。
- 先前结算 `+0` 的观察存在动画时序因素；代码会从零播放到最终值，尚未证实为持久显示错误，未修改经济逻辑。

## 2026-09-06 稳定性补充

- DeepSeek 上游等待上限设为 5.5 秒；超时返回 `MODEL_TIMEOUT`，客户端自动回退本地合法候选。
- 客户端超时、HTTP 失败、Worker 请求契约、GitHub Pages 来源配置均已纳入自动测试。
- Worker Dashboard 单文件已重新生成至 `cloudflare/ai-persona-worker/dist/worker.js`。
- 根目录全量自动测试和 Worker 子项目测试全部通过。
- 本机 Wrangler 依赖安装因 npm 网络长时间无响应已终止，因此本次仅完成待发布产物，尚未替用户更新 Cloudflare 线上部署。
