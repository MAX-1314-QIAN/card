# 《人格牌》AI 人格行为快照 V1

## 目的

行为快照是正式游戏数据和未来联网 AI 之间的隔离层。AI 不读取完整活动局存档，而是只接收程序整理好的聚合事实。

当前快照会在 N04、N08、N12 人格生成节点首次打开时创建，并写入对应节点存档。读档时沿用原快照，不重新统计，也不会因为刷新改变生成依据。

## 两个统计窗口

- `cumulative`：从本局开始到当前生成节点之前的全部战斗。
- `recent`：最近三场战斗，用于判断玩家近期是否改变打法。

N04 截止第 3 场、N08 截止第 6 场、N12 截止第 9 场。截点之后的数据不会倒灌进旧节点快照。

## 快照包含什么

### 牌型与得分

- 每种牌型使用次数；
- 每种牌型累计得分、平均得分和最高得分；
- 出牌次数占比和得分占比；
- 主要牌型、次要牌型和前两种牌型；
- 使用过的牌型数量；
- 连续使用相同牌型的次数与最长连续段；
- 单手平均得分和最高得分。

### 花色、点数与出牌结构

- 四种花色在计分牌中的数量和占比；
- 低点牌、中点牌、人头牌、A 的数量和占比；
- 平均提交牌数；
- 平均计分牌数。
- 出牌张数、计分牌张数和出牌前手牌张数的分布；
- 普通/稀有牌型、顺子、同花、同点数结构的实际出现率；
- 花色种类、牌型优先级、连续同型、切换牌型和首次牌型的实际出现率。

### 弃牌行为

- 弃牌次数；
- 弃牌总张数；
- 平均和最大单次弃牌张数；
- 弃牌后的下一手出现次数；
- 弃牌后下一手的平均得分。
- 各单次弃牌张数的分布，用于估算弃牌触发和成长条件。

### 人格贡献

- 每个人格实例的触发次数；
- 人格参与贡献的手数；
- 累计增加的筹码、倍率、独立倍率和金币；
- 当前是否装备以及装备槽位；
- 已装备但从未触发的人格也会保留，避免 AI 误以为玩家没有带它。

### 商店与主动养成

- 商店进入和刷新次数；
- 免费刷新、付费刷新和刷新花费；
- 金币获得、金币消费；
- 各类商品购买次数与花费；
- 玩家主动选择的花色、牌型和人格强化目标；
- 牌库修改次数；
- 人格第二、第三属性解锁记录；
- 当前花色强化、牌型强化和人格主词条强化等级。

## 快照明确不包含什么

- 完整活动局存档；
- 具体卡牌 UID；
- 完整手牌或牌库内容；
- 商店购买结果文案；
- 原始事件日志；
- 人格完整条件、效果和动态模板；
- 玩家输入的自由文本。

单卡强化目标会统一转换为 `CARD_TARGET`，只告诉 AI 玩家强化过单卡，不暴露具体卡牌实例。

## 数据置信度

快照会记录样本量和数据完整度：

- `LOW`：战斗或有效出牌样本不足；
- `MEDIUM`：至少 3 场战斗、6 次有效出牌；
- `HIGH`：至少 9 场战斗、18 次有效出牌。

旧存档如果缺少逐手计分牌或行动先后顺序，快照仍可生成，但会加入：

- `PARTIAL_CARD_GROUP_DATA`；
- `PARTIAL_ACTION_ORDER_DATA`；
- `INCOMPLETE_BATTLE_WINDOW`。

候选组合器会按置信度混合白名单预估触发率和玩家实际触发率。样本不足时不会让少量数据完全取代配置基准；第 3、6 关仍按既定桥接/破局顺序，第 9 关按既定顺势方向生成。

## 当前状态

行为快照已经接入节点存档，但 AI 生成人格仍未启用。当前 N04、N08、N12 继续使用已有本地原型人格，不改变玩家体验。

本地候选组合器现在会读取：

1. 本行为快照；
2. AI 人格 V1 白名单；
3. 当前装备、本局获得和永久收藏的人格机制指纹。

它已经能够先完成动态条件解析、触发率估算、数值预算过滤和候选复验。下一步是把候选机制指纹接入全范围查重。
