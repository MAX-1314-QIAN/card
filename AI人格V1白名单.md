# 《人格牌》AI 人格 V1 白名单

## 1. 本阶段做到哪里

当前已经完成“AI 可以选择什么”的本地合法零件表，以及“本地如何先生成安全候选”的第一版闭环；这仍不是联网 AI 接入。

- 新开的正式局已启用本地安全生成：`runtimeEnabled: true`、`activationMode: LOCAL_V1`。
- AI 将来只能返回零件 ID，不能返回自由数值、代码或未知规则。
- 每张候选固定由 1 个触发零件、1 个主效果零件和 1 个成长零件组成。
- 第 3、6、9 关结束后各生成 1 张；第 3、6 关的桥接/破局顺序每局随机互换，第 9 关固定顺势。
- 桥接、破局、顺势只用于内部选牌，正式界面不显示这些标签。
- 第二、第三词条池已经确认并接入本地运行：第二词条为半档强化，第三词条为一档强化，同一张人格的两条属性类型互斥。
- 玩家行为会先被整理成不含完整存档、卡牌实例 ID 和自由文本的版本化快照。
- 本地程序会解析动态牌型、估算真实触发率、过滤超预算组合，再输出最多 24 个候选。
- 候选构建器尚未替换正式奖励，现有游戏流程和正式人格获取结果不变。

## 2. 体验预期

玩家拿到的 AI 人格应该满足四件事：

1. 一眼看懂怎么触发，不出现“条件成立时”这类空话。
2. 立即有用，同时能在本局继续成长。
3. 和玩家行为有关，但不会机械复制已经装备的人格。
4. 任何生成错误都能被本地拦截，并改用合法备用人格。

## 3. 节点拆解

| 生成节点 | 对应进度 | 内部方向 | 玩家看到什么 |
| --- | --- | --- | --- |
| N04 | 第 3 关结束 | 桥接或破局 | 1 张新人格 |
| N08 | 第 6 关结束 | 与 N04 相反，保证桥接/破局各一次 | 1 张新人格 |
| N12 | 第 9 关结束 | 顺势 | 1 张新人格 |

N04 与 N08 的先后顺序必须在本局第一次确定时存档；刷新或读档不能重新交换。每个节点只生成一次，不提供刷新人格。

## 4. 数据来源与假设

### 已确认数据

- 本地条件执行器当前支持的触发类型。
- 本地效果执行器当前支持的筹码、倍率、独立倍率和最终乘区效果。
- 现有商店“人格主词条强化”商品 `SHOP_SERVICE_006` 的等价强化量：
  - `+10 筹码`
  - `+0.3 倍率`
  - `+10% 独立倍率`
- 每张 AI 人格必须成长；核心触发与初始主属性跨局保存，成长层数和第二、第三词条每局重置。
- 生成节点和桥接/破局/顺势安排。
- AI 人格第二、第三词条使用筹码、倍率、独立倍率三类本地白名单；数值分别为半档和一档，按当前经济配置以 4 / 6 金币解锁，同卡属性类型互斥。

### 暂定假设

- 各触发条件的预计触发率。
- N04、N08、N12 的单手期望价值预算。
- 成长层数上限与每层成长强度。
- 高品质相似人格的提升幅度和品质概率。

以上暂定数值继续标记为 `PROTOTYPE_ASSUMPTION` 或 `UNDECIDED`，方便后续用实测遥测校准；启用本地生成不代表这些参数已经完成最终平衡。

## 5. 中间推导：统一价值锚点

当前用现有商店同价商品建立一个可追溯的价值单位：

> 1 价值单位 = 10 筹码 = 0.3 倍率 = 10% 独立倍率 = 最终倍率增加 0.1

因此所有强度档位都只保存“价值单位”和按锚点换算后的具体数值：

| 强度档位 | 价值单位 | 筹码 | 倍率 | 独立倍率 | 最终倍率 |
| --- | ---: | ---: | ---: | ---: | ---: |
| AI_VALUE_050 | 0.5 | 5 | 0.15 | 5% | ×1.05 |
| AI_VALUE_100 | 1 | 10 | 0.3 | 10% | ×1.10 |
| AI_VALUE_150 | 1.5 | 15 | 0.45 | 15% | ×1.15 |
| AI_VALUE_200 | 2 | 20 | 0.6 | 20% | ×1.20 |
| AI_VALUE_300 | 3 | 30 | 0.9 | 30% | ×1.30 |
| AI_VALUE_400 | 4 | 40 | 1.2 | 40% | ×1.40 |

候选的初始期望价值按下式检查：

```text
初始单手期望价值 = 基础强度单位 × 预计触发率
成熟单手期望价值 =（基础强度单位 + 每层强度单位 × 成长上限）× 预计触发率
```

这不是最终平衡公式，而是 V1 的第一道超标过滤器。以后接入真实行为遥测后，触发率档位必须被实测数据替换。

候选构建时不会立刻完全相信少量对局数据，而是把白名单预估值和玩家实测值做加权：

```text
有效触发率 = 配置预估触发率 ×（1 - 数据权重）+ 玩家实测触发率 × 数据权重
```

当前低置信度不采用玩家实测值，中置信度权重为 50%，高置信度权重为 75%。这样第 3 关后的少量样本不会把人格强度拉偏，第 9 关后又能更明显地响应已经形成的打法。权重仍属于待实测参数。

## 6. 最终白名单配置

### 6.1 触发零件

V1 共包含 20 类触发零件、38 个可选变体，全部能由当前本地条件执行器表达。

| 类别 | 已开放内容 |
| --- | --- |
| 出牌数量 | 至少、至多、恰好打出指定张数 |
| 计分牌数量 | 本次计分牌达到 2～5 张 |
| 手牌风险 | 当前剩余手牌低于指定数量 |
| 牌型强度 | 达到指定牌型优先级、普通/稀有牌型 |
| 行为适配 | 玩家主要牌型、次要牌型、前两种常用牌型 |
| 出牌历史 | 连续相同牌型、切换牌型、本局首次牌型 |
| 牌型结构 | 顺子、同花、同点数结构 |
| 花色结构 | 一手牌含至少 2～4 种花色 |
| 弃牌状态 | 弃牌后的下一手、上一手后没有弃牌 |

行为适配触发不会把自然语言直接交给运行时。生成器只能选择 `BEHAVIOR_DOMINANT_HAND_TYPE`、`BEHAVIOR_SECONDARY_HAND_TYPE` 或 `BEHAVIOR_TOP_TWO_HAND_TYPES`，再由本地程序解析成真实牌型 ID。

每类触发都保存一条机械文案模板，例如“本次计分牌达到 4 张”“一次弃掉至少 3 张牌后的下一手”。正式界面不依赖 AI 临时解释触发条件。

### 6.2 主效果零件

V1 允许四类主效果：

- 增加筹码；
- 增加倍率；
- 增加独立倍率；
- 乘算最终倍率。

主效果只能引用已登记强度档位。最终乘区的可选上限低于普通筹码/倍率，避免低频条件直接组合出过大的爆发。

### 6.3 成长零件

V1 共包含 6 类成长方式：

- 核心条件每次触发时成长；
- 本局首次打出一种牌型时成长；
- 打出与上一手不同的牌型时成长；
- 打出至少 3 种花色时成长；
- 打出同点数结构时成长；
- 一次弃掉至少 2 张牌时成长。

成长统一增加 `growthStacks`，允许 3～5 层上限，每层使用 0.5 或 1 价值单位。具体组合仍需同时通过节点成熟预算。

最终乘区人格的成长不会直接把 `valuePerStack` 填到 `MULTIPLY_FINAL`。当前执行器会把该字段当成完整乘数，直接使用会产生错误结果。因此它的成长部分会安全映射到 `ADD_XMULT_RATE`，核心主属性分类仍保持独立倍率。

### 6.4 节点预算

| 节点 | 初始单手期望价值上限 | 成熟单手期望价值上限 | 状态 |
| --- | ---: | ---: | --- |
| N04 | 0.9 | 1.8 | 暂定，待实测 |
| N08 | 1.0 | 2.0 | 暂定，待实测 |
| N12 | 1.1 | 2.2 | 暂定，待实测 |

后获得的人格初始预算略高，用来补偿剩余成长战斗较少；但三档差距受到限制，不让第 9 关奖励直接淘汰前两张人格。

## 7. 本地校验

白名单加载时会检查：

- 是否仍保持关闭状态；
- 是否只使用当前执行器支持的条件和效果；
- 三个内部方向是否被隐藏；
- N04/N08 是否桥接与破局随机互换，N12 是否固定顺势；
- 每张人格是否强制包含成长零件；
- 强度数值是否严格由现有商店价值锚点换算；
- 动态牌型是否只使用批准的数据选择器；
- 运行时标记是否声明默认值和作用域；
- 第二、第三词条是否严格使用各自槽位白名单，且不会抽到相同属性类型；
- AI 是否只能返回 ID，并且本地备用结果是否强制存在。

任一项失败，整个配置无法通过项目配置校验。

## 8. 本地候选生成结果

当前候选生成器会按以下顺序工作：

1. 读取 N04、N08 或 N12 首次进入时锁定的行为快照。
2. 把“主要牌型”“次要牌型”“前两种常用牌型”解析成真实牌型 ID。
3. 只保留适用于当前内部方向的触发、主效果和成长零件。
4. 用累计数据与最近三场数据估算触发率和成长速度。
5. 同时检查基础强度、成长上限和成熟强度，超预算组合直接淘汰。
6. 生成可直接转成人格模板的声明式运行结构，并再次由独立校验器复算。
7. 按方向匹配度、可触发性和预算利用率排序，并限制同类零件扎堆。
8. 最多保留 24 个本地合法候选，供后续查重、备用生成器或联网模型选择。

每个候选已经包含清晰的触发条件、主效果、成长说明、运行时条件、成长规则、数值预算结果和机制指纹。桥接、破局、顺势只参与内部排序，不进入玩家文案。

最终乘区人格的基础效果仍可使用 `MULTIPLY_FINAL`，但逐层成长会自动改用 `ADD_XMULT_RATE`，避免执行器把每层增量误当成完整乘数。

## 9. 下一阶段

目前已完成行为快照、本地候选构建器、数值预算器、候选机制指纹、分层相似度模块、生成编排和后端选择边界。查重范围已经覆盖当前装备、本局拥有、局内放弃记录与永久收藏；当前装备和本局人格的阻挡权重最高。

新开的正式局会在 N04、N08、N12 使用本地安全生成。合法候选 ID 可以被采用；模型尚未连接、超时、返回非法 ID 或返回过期请求时，系统选择本地第一候选；没有独特候选时退回既有人格库。N04/N08 的桥接与破局分配一次生成并存档，N12 固定顺势。

正在进行且没有 `personaGenerationMode` 的旧存档恢复为 `LEGACY_PROTOTYPE`，继续领取旧原型人格，避免半局切换规则。新局固定写入 `LOCAL_V1`。联网模型以后只接在候选选择接口上，不改变人格执行、商店、收藏或存档结构。

后续还需要按顺序完成：

1. 用正式试玩数据校准触发率、成长上限和节点预算。
2. 确定后端地址、鉴权方式、超时时间和请求频率限制。
3. 接入后端 AI，让模型只在本地合法候选 ID 中做选择。

在前三步完成之前，不应把联网模型放进正式奖励流程。

“永久收藏人格连续三局未使用后，可出现更高品质的相似版本”已经预留显式开关，但目前品质增幅仍是 `UNDECIDED`。因此当前开关保持关闭：完全重复仍会被拦截，不会用未确认的品质规则绕过查重。
